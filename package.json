{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "A utility to shorten a given url.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/src/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sungly/url-shortener-.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/sungly/url-shortener-/issues"
  },
  "homepage": "https://github.com/sungly/url-shortener-#readme",
  "dependencies": {
    "body-parser": "^1.18.2",
    "convert-radix64": "^0.2.0",
    "cors": "^2.8.4",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2"
  }
}
